<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dreamspace.uucampus">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:theme="@style/AppTheme">
        <activity
            android:name=".ui.person.PersonInfoActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.person.MyUseless"
            android:label="@string/z_my_useless" />
        <activity
            android:name=".ui.person.MyCollection"
            android:label="@string/z_my_collection" />
        <activity
            android:name=".ui.person.ApplyStore"
            android:label="@string/apply_store" />
        <activity
            android:name=".ui.person.MyStore"
            android:label="@string/main_store" />

    </application>

</manifest>
